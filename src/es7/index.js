    // Class: Que se implemento en es7x ? - v6
    // Version junio de 2016


    //  METHOD INCLUDES
    // Trabaja sobre un arrego o un string. Permite saber si hay un elemento dentro de ese valor.

    let arrayNumbers = [1,2,3,7,8];
    if (arrayNumbers.includes(9)) {
        console.log('El numero ha sido hubicado');
    } else {
        console.log('Desafortunadamente no hemos encontrado el número que buscas')
    }


    // ELEVAR A LA POTENCIA

    let base = 4;
    let exponent = 3;
    let result = base ** exponent;
    console.log(result);
